import { createContext } from "react";

export const ProductContext = createContext(); // Create a new context for products, which will be used to share product data across components without prop drilling.