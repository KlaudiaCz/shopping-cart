import { createContext } from "react";

export const CartContext = createContext(); // Create a new context for the shopping cart, which will be used to share cart data and functions across components without prop drilling.